# 프로젝트 규칙

매번 말하지 않아도 항상 지켜야 하는 규칙이다.

## 파일 경로
- 파일 경로를 코드에 하드코딩하지 않는다.
- 사용자가 그때그때 업로드한 파일을 사용한다.

## 데이터 파일 (엑셀·CSV)
- 엑셀·CSV 파일은 앱 폴더 밖에 둔다.
- `.gitignore`에 엑셀·CSV 확장자(`*.xlsx`, `*.xls`, `*.csv`)를 추가해 커밋에서 제외한다.

## API 키
- API 키는 코드에 직접 넣지 않는다.
- `.env` 파일에서 읽어 사용한다.
- `.env`도 `.gitignore`에 포함한다.

## 언어
- 답변은 항상 한국어로 한다.
